name: real_life_side_quest
description: A gamified task management app that turns your goals into quests.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.21.0
  firebase_auth: ^4.14.0
  cloud_firestore: ^4.14.0
  provider: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
